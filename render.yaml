services:
  - type: web
    name: staybook
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        value: postgres://postgres:postgres@localhost:5432/hotel
        sync: false
      - key: NODE_ENV
        value: production
        sync: false
